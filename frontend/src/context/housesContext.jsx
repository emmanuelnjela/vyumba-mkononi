import { createContext } from "react";

// this will hold all the data and functions related to houses

const HousesContext = createContext()

HousesContext.displayName = "HousesContext"

export default HousesContext